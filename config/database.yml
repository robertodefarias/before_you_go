default: &default
  adapter: postgresql
  encoding: unicode
  max_connections: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: before_you_go_development

test:
  <<: *default
  database: before_you_go_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
